target "OAuth2Client.framework" do

end

target "OAuth2Client" do
  platform :ios, "5.0" #that shit cray!

  target "OAuth2ClientTests" do
    inherit! :search_paths

    pod 'Specta',      '~> 0.2'
    pod 'Expecta',     '~> 0.3'
    pod 'OCMock',      '~> 3.1'
    pod 'OHHTTPStubs', '~> 3.1'
  end
end
